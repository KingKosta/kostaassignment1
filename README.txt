Things done in the HTML file:
-Created a link from the title "search engine optimization" to direct you to the place on the page which describes "search engine optimization"
-Changed website title
-Changed a few "div" to "header", "nav", "class" to make it more clear
-Created alternate title for images

Things done in the CSS file:
-Cleaned up the lines of code
-Used hastags to group same items with same parameters
-Colour codes were slightly off, changed them to match with each other
